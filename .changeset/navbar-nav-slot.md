---
"@juz/design-system": minor
---

`Navbar`: add `nav` prop — a slot rendered between the logo/breadcrumb section and the central search field, intended for the app's main top-level navigation (e.g. dropdown menus). Hidden below `md` so it doesn't compete with mobile layout; search keeps its `flex-1` and shrinks as needed.
